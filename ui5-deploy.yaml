# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: '2.6'
metadata:
  name: fioriappprac
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        ignoreCertError: true 
        target:
          url: https://54.228.55.175:44300/
          client: "100"
        credentials:
          username: env:UI5_USERNAME
          password: env:UI5_PASSWORD
        app:
          name: ZFIORIAPPPRAC
          description: Fiori App prac - AT Final
          package: ZMRI_PRAC
          transport: S4HK901524
